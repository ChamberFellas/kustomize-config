apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: notifications-service
    path: patch.yaml

images:
  - name: weeturtle/notifications
    newTag: bae50f6
